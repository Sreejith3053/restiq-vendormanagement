const admin = require('firebase-admin');

// Initialize Firebase Admin (assuming default credentials or emulator if local)
// Since this is just a quick check, let's use the local firebase setup if possible,
// or we can just look at the app source code to see how they query vendor items.
